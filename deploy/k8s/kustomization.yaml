apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: inference
resources:
  - 00-namespace-config.yaml
  - 10-redis.yaml
  - 20-api-gateway.yaml
  - 30-scheduler.yaml
  - 40-inference-worker.yaml
  - 50-metrics.yaml

images:
  - name: pytorch-inference-platform
    newTag: latest

# Apply with:
#   docker build -t pytorch-inference-platform:latest .
#   kubectl apply -k deploy/k8s
